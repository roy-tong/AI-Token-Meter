# 2026-09-06：0.3.0 双平台更新发布

关联需求：`REQ-20260906-004`。用户明确要求发布，让已有机器可在应用内更新。

## 发布决定与顺序

目标0.3.0/build 11，不能继续只发Preview，否则现有macOS稳定appcast无法发现。沿用现有双平台发布流程，不更换验证公钥，不导出或记录私钥。

1. 同步根VERSION、两个plist、Windows npm/Cargo/Tauri以及README/CHANGELOG/Release Notes。
2. 运行完整验证，干净main打包签名macOS ZIP，创建同版本tag和草稿Release。
3. 标签上的macOS与Windows CI都通过后，由workflow公开双平台资产。
4. 最后推进稳定appcast与固定Windows Preview feed；下载线上资产核对SHA-256及两种签名，验证更新清单中版本和链接。
5. 完成后补发布节点、线上校验和、日志及需求状态。真实桌面项目与间歇性测试问题继续保持各自状态。

## 当前状态

2026-09-06 21:35（Asia/Singapore）已公开发布 [v0.3.0](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.3.0)，不是草稿或 prerelease，GitHub latest 同样为本版。标签指向 `bb215c3`；双平台发布 workflow [34035797098](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34035797098) 的 macOS 验证、Windows 测试/签名构建和同步发布全部成功。稳定 appcast 提交为 `f9a1f83`。

发布前检查发现Swift/Rust版本断言及Windows初始更新状态仍保留preview.3，已同步到0.3.0/build 11；不是运行功能回归，不移除版本一致性检查。macOS403项、Windows前端51项和12项生命周期/真实Chrome样式门禁通过；Rust重新完整复验后进入打包。

本机Rust179项与Clippy零警告复验通过。正式打包入口再次完整运行macOS403项、文档与公开安全检查；Release资源、Sparkle嵌入、严格签名、ZIP EdDSA和篡改副本拒绝测试均通过。未导出或更换任何签名私钥。

## 公网资产与更新源验收

匿名从公开 Release 重新下载全部七份资产，不使用登录凭证。首次下载因本机网络慢而超时，续传后以完整 SHA-256 与签名验证通过为准，未重建或替换任何公开资产。

| 安装包 | 大小（bytes） | SHA-256 |
| --- | ---: | --- |
| `AI-Token-Meter-0.3.0-macOS-arm64.zip` | 3563207 | `cf18d42a167eb939ca448fcd77a24667524ce26e69a56edda5c56dc030c69af8` |
| `AI-Token-Meter-0.3.0-windows-x64-setup.exe` | 5034291 | `9aa53f7ab0c7ff492fbc22cce94d87ffc1b6a1602b4986380e7695748c81e4e3` |

- 两份安装包均与公开 `.sha256` 一致。
- 公开 macOS ZIP：Sparkle EdDSA 验证通过、篡改副本被拒绝；解压后严格递归 codesign 验证通过，版本 `0.3.0`、build `11` 与 appcast 一致。
- 公开 Windows EXE：使用应用内嵌公钥的 Rust 验证器校验 `.exe.sig` 成功；Windows CI 同时验证 GUI subsystem 与真实 NSIS。
- [稳定 appcast](https://raw.githubusercontent.com/sljzdotcom/AI-Token-Meter/main/appcast.xml) 与 Release appcast 字节一致，首项为 `0.3.0` / `11`，公开下载 URL 有效。
- [Windows 稳定清单](https://github.com/sljzdotcom/AI-Token-Meter/releases/latest/download/latest.json)、[旧 Preview 固定清单](https://github.com/sljzdotcom/AI-Token-Meter/releases/download/windows-preview-feed/latest-preview.json) 与本版 Release `latest.json` 字节一致，均为 `0.3.0`，URL 指向已验签安装包。
- 本机已安装的 `0.3.0-preview.0`（build `7`）通过真实 Settings → About → Check for Updates 显示 **Version 0.3.0 is available**，**Update Now** 按钮启用；未点击安装、未覆盖应用。由此验证旧版发现新版的实际链路；不冒充本次完成了 Windows 真机原位升级。

需求 `REQ-20260906-004` 的发布、发现和公开签名资产验收完成；当前 README、安装指南、项目状态、发布流程、版本历史和日志索引同步更新。阶段结束再读取需求列表，无其他可在当前环境继续执行的待处理项；既有真机/证书/间歇性时序项目保持原状态。

## 安全与验收边界

macOS验证包不含Widget，仍ad-hoc签名且未公证；Windows安装器没有Authenticode，但Tauri更新内容必须通过签名验证。不得把发布成功等同于全量Windows真机验收通过。
